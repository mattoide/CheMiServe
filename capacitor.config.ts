import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.matto.listaspesa',
  appName: 'ListaSpesa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
